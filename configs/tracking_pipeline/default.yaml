_target_: human_detection.pipeline.TrackingPipeline

config:
  _target_: human_detection.pipeline.TrackingPipelineConfig

  bbox_enabled: ${results.bbox.enabled}
  bbox_path: ${results.bbox.path}

  video_enabled: ${results.video.enabled}
  video_path: ${results.video.path}

  debug_enabled: ${results.debug_video.enabled}
  debug_path: ${results.debug_video.path}

  display_enabled: ${results.display.enabled}
  display_window_name: ${results.display.window_name}
